## 🧠 Project Overview

**🎯 Goal:**  
Build a **fully responsive, visually engaging expense tracking website** called **FinMate** using:

- **HTML, CSS (with animations)** for the frontend  
- **Vanilla JavaScript** (No frameworks)  
- **Firebase** (Authentication + Firestore database) for the backend  
- **Chart.js** for analytics and visualizations  

**🎯 Target Audience:** Indian users (Currency: INR ₹)

---

## 🗂️ File & Folder Structure

```
/project-root
│
├── index.html               ← Landing page
├── /html                    ← All other pages
│   ├── login.html
│   ├── signup.html
│   ├── dashboard.html
│   ├── transactions.html
│   ├── insights.html
│   ├── profile.html
│   ├── budgeting.html
│
├── /css                     ← Split CSS files by purpose
│   ├── global.css
│   ├── landing.css
│   ├── auth.css
│   ├── dashboard.css
│   ├── transactions.css
│   ├── insights.css
│   ├── profile.css
│   └── components.css       ← Cards, buttons, footer, navbars
│
├── /js
│   ├── firebase-config.js   ← Contains Firebase credentials
│   ├── auth.js
│   ├── dashboard.js
│   ├── transactions.js
│   ├── insights.js
│   ├── profile.js
│   ├── budgeting.js
│   └── utils.js             ← Reusable helper functions
│
└── /assets
    ├── /images              ← Logos, icons
    
```

---

## 🔐 Firebase Integration (Auth + Firestore)

- Include Firebase SDK scripts in `index.html`
- Create and import `firebase-config.js` for Firebase initialization
- Use the Firestore structure below:

```
users/
  {uid}/
    transactions/
      {autoDocId}
    meta/
      income: { amount, type, currency: "INR" }
      budgets: { category: amount }
```

- Enable **Email/Password Auth** and **Firestore DB** in Firebase Console

---

## 🌐 Pages & UI Details

### 📌 Landing Page (`index.html`)

**Sections:**
- **Hero Section** with title 
-   and gradient CTAs ("Login", "Sign up") in the header with logo and logotitle in top left
- **Feature Highlights** using rounded cards and icons (Secure, Real-time, Visual Insights, Budgeting)
- **Why Choose Us**: bullet-style trust points (privacy, INR, responsive)
- **Testimonials**: horizontal slider
- **Footer** (reusable across all pages)

**Design:**  
- Light theme base  
- Primary: `#00bcd4` (Light Blue)  
- Accent Gradient: Blue → Soft Purple (`#b388ff`)  
- Font: Poppins / Inter  
- Cards: `rounded-2xl`, subtle shadows  
- Animations: button hover, slide-ins, modals  

---

### 🔐 Auth Pages (`login.html`, `signup.html`)
- Firebase Email/Password auth
- Firebase `onAuthStateChanged` to manage state
- “Forgot Password” with recovery
- Redirect to `dashboard.html` on login
- these two html pages will also have the same header layout as index.html file with logo and login/signup buttons**

---

### 📊 Dashboard Page (`dashboard.html`)
- Side Navbar (slide-in/out on toggle click):
  - Dashboard
  - Transactions
  - Insights
  - Budgets
  - Profile

- Fixed Header:
  - Logo + FinMate title (left)
  - Navbar toggle icon (fixed) in left of logo and then leave a space of that much that will be equal to the space aligned with navbar below it and where the navbar width will end from there the logo will be in header 
  - Profile icon (top-right dropdown → Profile, Logout, Dashboard)

- Main Content:
  - Welcome: "Hey, [UserName]!"
  - Summary Cards:
    - 💸 Total Income
    - 📉 Total Expenses
    - 💰 Current Balance
  - Quick Glance Chart (Toggle: Pie / Bar via Chart.js)

---

### 📁 Transactions Page (`transactions.html`)
- Table/List View:
  - Date | Description | Category | Type | Amount | (Edit | Delete) as action buttons
- Filters:
  - Category Dropdown
  - Date Range Picker
  - Type: Income/Expense
- Edit action button should open the modal to edit the whole transaction and save changes  and delete action button should open a confirmation modal and then delete 
-  at the top right of this page below header give a button to add transaction and then it shoukd open a modal for all the details like an input form and then save transaction and then the transaction table should be updated with new trnsaction. the code for this add-transaction button will be written in transaction.html file with the script logic in transactions.js file 
---
Add Transaction button description (`transaction.html`)
- Form:
  - Amount
  - Date Picker
  - Category (Dropdown + Add New category )
  - Description (Textarea)
  - Type: Income / Expense
- On submit: Save under `users/{uid}/transactions`
- Show toast/snackbar + form validations

---

### 📊 Insights Page (`insights.html`)
- Charts:
  - Pie Chart → Expenses by Category
  - Bar Graph → Income vs Expense (monthly)
  - Line Graph → Monthly Balance Trend
- Filters:
  - Date Range
  - Type
- Animations on filter changes

---

### 👤 Profile Page (`profile.html`)
- View/Edit:
  - Name
  - take Income Input (Amount + Dropdown: Monthly/One-Time)
  - “Set Income” CTA
- Store income under:
  ```
  users/
    uid/
      meta/
        income: { amount, type, currency: "INR" }
  ```
- some Analytics to appear on profile page:
  - Top 3 Spending Categories
  - Month-on-month change
  - Savings Trend
  - Budget Breach Alerts (if enabled)

---

### 🎯 Budgeting Page (`budgeting.html`)
- Set spending limits per category
- Alert user visually when exceeding the limiting with toast notifications 

---

## 🌟 Global UI/UX Guidelines

- **Base**: White
- **Primary Color**: `#00bcd4` (Light Blue)
- **Accent Gradient**: Blue → Soft Purple (`#b388ff`)
- **Font**: Poppins / Inter
- **Buttons**: Gradient + shadow on hover
- **Spacing**: Use `rem`/`%`, padding: `p-4`/`p-6`
- **Responsive**: Mobile-first using media queries
- **Animations**:
  - Navbar slide-in/out
  - Modal fade-in
  - Chart animations
  - Button hover glows

---

## 🔗 Additional Notes for Windsurf AI

- Use **separate HTML files** per page for maintainability
- **Footer remains the same** across all pages (reusable component)
- **Header differs**:
  - `index.html`, `login.html`, `signup.html`: logo + auth buttons
  - Authenticated pages(all other html pages ): logo + toggle + profile dropdown(detailed description of header across these pages
 Fixed Header:
  - Logo + FinMate title (left)
  - Navbar toggle icon (fixed) in left of logo and then leave a space of that much that will be equal to the space aligned with navbar below it and where the navbar width will end from there the logo will be in header 
  - Profile icon (top-right dropdown → Profile, Logout, Dashboard)) 

- Maintain **clean folder and file references** in all HTML files
- Ensure **semantic HTML**, accessibility, and responsive behavior throughout
- Provide **modular, readable, and well-commented** CSS and JS

